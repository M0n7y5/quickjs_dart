name: quickjs_dart
description: Pure Dart FFI wrapper around Bellard's QuickJS JavaScript engine.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  code_assets: ^1.2.0
  ffi: ^2.1.0
  hooks: ^2.0.0
  logging: ^1.2.0
  native_toolchain_c: ^0.19.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
